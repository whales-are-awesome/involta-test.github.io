type Sizes = 'lg' | 'md';
type Views = 'primary' | 'switch';
type Align = 'center' | 'start';
type Types = 'checkbox' | 'radio';
type Themes = 'primary';
type Wrappers = 'button';

export {
    Sizes,
    Views,
    Align,
    Types,
    Themes,
    Wrappers
}
